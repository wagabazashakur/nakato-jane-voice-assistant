// API keys for the Nakato Jane voice assistant.
// Leave these blank to avoid exposing your keys in the client. The assistant
// will prompt you for a key on first use and store it in your browser’s
// localStorage so it isn’t embedded in the source code.

window.OPENAI_API_KEY = '';
window.AZURE_TTS_KEY = '';
window.AZURE_REGION = '';
